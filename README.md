# Portfolio (Node.js)

This is a simple static portfolio site served with Node.js + Express.

## Run locally

```bash
npm install
npm run dev
```

Then open `http://localhost:3000`.

## Deploy

Push to GitHub and deploy with your preferred platform (Render, Railway, Vercel, etc.).